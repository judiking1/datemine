{
  "name": "datemine",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "check:drafts": "node scripts/check-drafts.mjs",
    "progress": "node scripts/progress.mjs",
    "build:drafts": "node scripts/build-draft-preview.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.6.0"
  }
}
