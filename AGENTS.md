# datemine — Agent Contract

이 저장소에서 작업하는 모든 에이전트(Codex/Claude 등)는 코드를 바꾸기 전에 이 문서를 읽는다.

## 필독 순서
1. `docs/PRODUCT.md` — 제품 정의·톤·데이터 2층 구조·법적 가드레일 (최상위)
2. `docs/ARCHITECTURE.md` — 모노레포 구조·도메인 타입·계층 경계
3. 이 문서(`AGENTS.md`) — 작업 규칙과 하드 게이트

## 제품 한 줄
매일 아침, 오늘이 무슨 날이고 어떤 발언·행동이 지뢰인지 세게 알려주는 경각심 캘린더 앱.

## 하드 규칙 (위반 시 서비스가 위법·불가 — 절대 타협 금지)

1. **데이터 2층 분리 (최우선).**
   - **Layer 1 (Raw, 실명 포함)** = 내부 연구용. **클라이언트 번들·공개 API·발행
     콘텐츠·로그·분석 이벤트에 절대 실리지 않는다.** 접근 통제된 비공개 저장소에만 존재.
   - **Layer 2 (Published, 실명 제거)** = 사용자에게 나가는 유일한 데이터.
   - 코드에서 Layer 1 타입이 앱 패키지나 public API 응답 타입으로 흘러가면 그 변경은
     거부한다. 타입 경계로 컴파일 단계에서 막는다.
2. **실명·개인식별정보 미발행.** Layer 2의 어떤 필드도 특정 개인을 지목/추정 가능하게
   하지 않는다. 게시 전 사람 검수 게이트를 통과한 콘텐츠만 발행 상태로 둔다.
3. **톤은 세게, 대상은 사람이 아니라 패턴.** 카피는 직설적·자극적으로 가되(순화 금지),
   특정인을 저격하지 않고 "반복되는 실패 유형"을 겨눈다.
4. **개인정보보호법 준수.** 공개 보도된 사실만 수집, 목적 외 사용·과다 보관 금지.
5. **역사·기념일 사실은 공적 사료 기반.** 추정·왜곡 금지.

## 아키텍처 원칙
- **데이터 = source of truth, UI·알림·공유카드는 파생 consumer.**
- core/domain 로직은 프레임워크·플랫폼 비의존(React Native·DB·HTTP에서 분리, adapter 경유).
- 타입은 공유 패키지 한 곳에서 정의하고 앱·백엔드가 함께 쓴다.
- 파괴적/외부 노출 작업(발행, 데이터 export)은 검수 게이트 뒤에서만.

## 기술 스택 (근본·안정·유지보수)
- 모노레포: pnpm workspaces + Turborepo, TypeScript strict 전면
- 앱: React Native + TypeScript (단일 코드베이스)
- 백엔드: Node + Hono(또는 NestJS), PostgreSQL, Drizzle(또는 Prisma)
- 콘텐츠 파이프라인: 뉴스 검색(Layer 1) → 유형화 → 실명제거·검수 → Layer 2 발행

## 커밋·검증 규칙
- Conventional Commits, bisectable 하게 분리(인프라→타입→서비스→UI→테스트→문서).
- 커밋 전: 타입체크 + 린트 + 테스트 통과. 특히 **Layer 1→Layer 2/클라이언트 유출
  방지 타입 경계 테스트**는 항상 green이어야 한다.
- 커밋/PR 메시지에 민감정보(실명 사례 원본, 출처 개인정보)를 남기지 않는다.

## 금지
- Layer 1(실명) 데이터를 앱·public API·리포지토리 히스토리에 커밋하는 것.
- 특정 개인을 지목하는 발행 카피 작성.
- UI 렌더 객체를 편집 가능한 source of truth로 사용하는 것.
- 근거 없는 역사/사실 서술.
